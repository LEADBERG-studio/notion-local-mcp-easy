{
  "id": "subagent",
  "display_name": "Remote model subagent",
  "version": "2.1.0",
  "entrypoint": "plugin.py",
  "supported_modes": [
    "read_only",
    "full_access"
  ],
  "capabilities": [
    "ai",
    "subagent",
    "prompt-testing"
  ],
  "config_schema_version": 1,
  "required_env": [],
  "dependencies": [],
  "required": false,
  "install_scope_support": "both",
  "tools": [
    {
      "name": "subagent_status",
      "title": "Subagent status",
      "description": "Report whether a remote model is configured and list open sessions. Connection details are never returned.",
      "capability": "subagent.status",
      "mode_required": "read_only",
      "input_schema": {
        "type": "object",
        "properties": {
          "target": {
            "type": "string",
            "description": "Configured target name. Omit to use the default target."
          }
        }
      },
      "output_schema": {
        "type": "object"
      },
      "handler_ref": "subagent.status",
      "safety_tags": [
        "ai",
        "read_only"
      ],
      "diagnostic_visibility": "redacted"
    },
    {
      "name": "subagent_list_targets",
      "title": "List subagent targets",
      "description": "List the configured remote model targets by name. Endpoints, keys and model ids are never returned.",
      "capability": "subagent.list_targets",
      "mode_required": "read_only",
      "input_schema": {
        "type": "object",
        "properties": {}
      },
      "output_schema": {
        "type": "object"
      },
      "handler_ref": "subagent.list_targets",
      "safety_tags": [
        "ai",
        "read_only"
      ],
      "diagnostic_visibility": "redacted"
    },
    {
      "name": "subagent_list_models",
      "title": "List subagent models",
      "description": "List the model ids the remote endpoint offers, plus the configured default. Use the returned ids with the model argument to switch without restarting. The endpoint and key are never returned.",
      "capability": "subagent.list_models",
      "mode_required": "read_only",
      "input_schema": {
        "type": "object",
        "properties": {
          "target": {
            "type": "string",
            "description": "Configured target name. Omit to use the default target."
          }
        }
      },
      "output_schema": {
        "type": "object"
      },
      "handler_ref": "subagent.list_models",
      "safety_tags": [
        "ai",
        "read_only"
      ],
      "diagnostic_visibility": "redacted"
    },
    {
      "name": "subagent_ask",
      "title": "Ask the remote model",
      "description": "Send one prompt to the configured remote model and return its reply. Use this for a single question with no follow-up.",
      "capability": "subagent.ask",
      "mode_required": "read_only",
      "input_schema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "description": "What to ask the remote model."
          },
          "system_prompt": {
            "type": "string",
            "description": "Optional system prompt for this call. Overrides the configured default."
          },
          "target": {
            "type": "string",
            "description": "Configured target name. Omit to use the default target."
          },
          "model": {
            "type": "string",
            "description": "Model id to use for this call. Omit to use the target default. List options with subagent_list_models."
          }
        },
        "required": [
          "prompt"
        ]
      },
      "output_schema": {
        "type": "object"
      },
      "handler_ref": "subagent.ask",
      "safety_tags": [
        "ai",
        "read_only"
      ],
      "diagnostic_visibility": "redacted"
    },
    {
      "name": "subagent_start",
      "title": "Start a subagent session",
      "description": "Open a multi-turn conversation with the remote model. Returns a session id to use with subagent_say.",
      "capability": "subagent.start",
      "mode_required": "read_only",
      "input_schema": {
        "type": "object",
        "properties": {
          "system_prompt": {
            "type": "string",
            "description": "System prompt for the whole session. This is the prompt under test."
          },
          "target": {
            "type": "string",
            "description": "Configured target name. Omit to use the default target."
          },
          "model": {
            "type": "string",
            "description": "Model id to use for this call. Omit to use the target default. List options with subagent_list_models."
          }
        }
      },
      "output_schema": {
        "type": "object"
      },
      "handler_ref": "subagent.start",
      "safety_tags": [
        "ai",
        "read_only"
      ],
      "diagnostic_visibility": "redacted"
    },
    {
      "name": "subagent_say",
      "title": "Send a turn to the subagent",
      "description": "Send the next turn in an open subagent session. History is kept on this side within a bounded rolling window.",
      "capability": "subagent.say",
      "mode_required": "read_only",
      "input_schema": {
        "type": "object",
        "properties": {
          "session": {
            "type": "string",
            "description": "Session id from subagent_start."
          },
          "prompt": {
            "type": "string"
          },
          "model": {
            "type": "string",
            "description": "Model id to use for this call. Omit to use the target default. List options with subagent_list_models."
          }
        },
        "required": [
          "session",
          "prompt"
        ]
      },
      "output_schema": {
        "type": "object"
      },
      "handler_ref": "subagent.say",
      "safety_tags": [
        "ai",
        "read_only"
      ],
      "diagnostic_visibility": "redacted"
    },
    {
      "name": "subagent_end",
      "title": "Close a subagent session",
      "description": "Discard an open subagent session and its history.",
      "capability": "subagent.end",
      "mode_required": "read_only",
      "input_schema": {
        "type": "object",
        "properties": {
          "session": {
            "type": "string"
          }
        },
        "required": [
          "session"
        ]
      },
      "output_schema": {
        "type": "object"
      },
      "handler_ref": "subagent.end",
      "safety_tags": [
        "ai",
        "read_only"
      ],
      "diagnostic_visibility": "redacted"
    }
  ]
}
